# Win Counter Match Gate

Persistent Win Score and Win Counter writes require valid live fighter snapshots for both teams while character select is inactive.

Automatic holds, Freeze writes, and direct win texture writes are paused outside that context.
