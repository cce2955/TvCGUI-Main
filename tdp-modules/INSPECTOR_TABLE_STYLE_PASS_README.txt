Inspector + table style pass

What changed
- Right inspector now uses softer section cards instead of flat stacked boxes.
- Header/hero block is cleaner and the helper text reads like contextual microcopy.
- Compare card now includes compact delta chips for Damage, Startup, Active, Hitstop, and Blockstun.
- Move table now shows compact text badges in the Move column:
  [N] normal  [S] special  [U] super  [P] projectile  [H] hit
- Child hit rows use a clearer branch marker (↳) instead of plain indentation.
- Table strip includes a small badge legend.
- Row/selection colors were tuned to feel more like a purpose-built lab tool and less like a form.

Notes
- No new scans or Dolphin reads were added.
- Compare remains cache-only.
- This is a styling/layout pass layered on top of the OTG quick-tile build.
