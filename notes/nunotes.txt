GNTA / MOT / SEQ Architecture Notes
High-level model

Attack behavior is split cleanly across three layers:

SEQ: logic and timing

MOT: skeletal pose over time

GNTA: spatial interpretation rules for gameplay

Hitboxes are never authored directly. They are computed at runtime.

SEQ (sequence / move logic)

SEQ defines when an attack happens, not where or how big it is.

Contains:

active frame windows

damage, knockback, hit reaction, flags

animation ID selection

SEQ is parsed once and compiled into internal engine structures.

After parsing, SEQ bytes are not referenced again.

SEQ is not hot-editable at runtime; only its compiled output in RAM is.

MOT (motion / animation)

MOT contains skeletal animation data only.

Defines bone transforms per frame at 30 FPS.

No hitbox data, no damage, no gameplay flags.

Supplies pose information used by the engine every frame.

MOT is animation-scoped and reused across moves and tiers.

GNTA (geometry / spatial interpretation)

GNTA is animation-scoped metadata, keyed 1:1 by animation ID.

GNTA data is loaded as part of the animation package (MOT-adjacent).

Standalone .gnta files are extracted representations of embedded animation metadata.

GNTA does not define hitbox geometry directly.

GNTA defines:

which bone or reference mode to use

how pose data should be interpreted

how spatial values are scaled or modulated

how parameters evolve over animation time

GNTA structure (confirmed)

GNTA files are record-based and struct-parsed.

Each record contains:

a fixed field offset (example: 0x10)

bone ID or sentinel

flags / mode selectors

normalized float coefficients

optional enable/disable gating

GNTA records can be time-variant and contain curves with keyframes.

GNTA curves are evaluated per animation frame in sync with MOT.

Important sentinel:

Bone ID = 0xFFFF indicates non-bone modes such as root or proximity.

Used for throws and body-wide checks.

GNTA floats are dimensionless scalars, not world-space values.

AnimationList.dat

Character-scoped file.

Explicit list of animation IDs valid for that character.

Determines which animation IDs (and thus GNTA records) are expected to exist.

Explains why some animations never produce hitboxes.

Confirms GNTA usage is intentional and curated per character.

Runtime hitbox computation

At runtime, per frame:

MOT supplies current pose.

GNTA supplies a scalar or curve value for the current frame.

Engine math derives distances from pose (bone to reference).

Final hitbox radius and offsets are computed as:

derived_value = pose_distance × gnta_scalar
clamped / modified by engine rules

Results are written to RAM.

RAM contains results, not source data.

This explains:

large throw radii

tiny jab radii

smooth frame-to-frame changes

tier differences tied to animation selection

Memory observations

GNTA scalar values (example: ~0.53333336) appear verbatim in RAM.

Clamp values (example: 0.5) appear heavily due to engine minimums.

Same behavior observed across characters, confirming engine-global math.

Editing implications

Real-time editing is only practical at the RAM level.

Editing GNTA or SEQ requires reloads.

GNTA editing changes rules, RAM editing changes results.

SEQ editing affects logic; GNTA editing affects spatial behavior; MOT editing affects pose.

Final mental model

SEQ answers when and what

MOT answers where the body is

GNTA answers how that pose becomes attack space

Engine code performs the math

RAM holds the final, editable output

Unpacking MOT for GNTA data

Although GNTA is often handled as standalone .gnta files during analysis, the engine treats GNTA as animation-bound metadata, not an independent asset.

In practice:

GNTA data is packaged alongside MOT data inside the animation container.

The animation loader resolves:

animation ID

MOT motion data

associated GNTA records

All three are loaded together and share the same timing domain.

Standalone .gnta files represent extracted GNTA blocks, not the original on-disk layout used by the game.

When unpacking MOT data:

GNTA records appear as structured, non-skeletal blocks adjacent to motion data.

GNTA records are not bone animation curves, even though they may be time-keyed.

GNTA uses its own schema:

fixed offsets

record tables

scalar or curve data

GNTA records reference animation time implicitly rather than storing absolute frame counts.

Because GNTA is animation-scoped:

There is no animation ID stored inside GNTA itself.

Identity is assigned externally by the loader based on:

animation slot

character animation table

This allows the same animation ID to exist across characters with different GNTA tuning.

When extracted for tooling:

GNTA is separated for inspection and editing.

MOT remains skeletal pose only.

Repacking requires preserving record order, offsets, and timing assumptions.

This packaging explains:

why GNTA curves line up exactly with MOT frame counts

why hitbox behavior updates immediately on animation change

why GNTA edits affect gameplay without altering animation visuals

GNTA is best thought of as animation-resident gameplay metadata, not a separate system layered on top of animation.
Vertex Buffer Analysis
Location

Vertex buffer base:
0x92468B20

Stride:
0xD0

Offsets referenced are inside the vertex layout and represent model-space data, not world-space.

Offset +0x28

Behavior:

Y span increases during jump

Example range: Y:[-0.434 .. 2.346]

X and Z remain symmetric around 0

Average center remains near (0, 0, 0)

Conclusion:

This is model-space vertex position (local Y relative to mesh origin).
Affected by animation.
Not affected by stage/world movement.

Offset +0x2C

Behavior:

X span shifts during cross-up

Value spikes during flip/facing change

Conclusion:

Likely adjacent vec3 component in local space.
Represents rotated or mirrored local-space axis during facing change.

Offset +0x30

Behavior:

Z occasionally spikes to ~10 during jump/landing

Conclusion:

Misaligned read crossing into unrelated float field.
Likely matrix or weight data.

Offset +0x34

Behavior:

Z behaves differently when grounded

Flattens to 1.00 during jump

Appears to vary relative to opponent distance

Conclusion:

Likely animation blend / root correction / foot IK data.
Not stage distance.

Offsets +0xB8 → +0xC4

Behavior:

Tight [-1 .. 1] ranges

Consistent normalization

Conclusion:

Normalized vectors.
Likely normals, tangents, or bone-space vectors.

Critical Vertex Conclusion

Average vertex center remains near zero at all times.

Therefore:

Vertex buffer is model-space

World position applied via GX transform matrix

Stage movement does not modify vertex buffer

Implication:

World position must exist in:

Fighter actor struct

Or transform matrix uploaded before draw

Vertex buffer unsuitable for stage tracking.

Actor Runtime Structure
Example Actor Base (Ryu Slot 0)

0x9246B9C0

Confirmed World Coordinates
World X

Offset:
BASE + 0x98
Example: 0x9246BA58

Behavior:

Far left wall ≈ -8.5 (0xC1080000)

Center ≈ 0

Far right wall ≈ +8.5 (0x41080000)

Smooth continuous change while walking

Symmetric around 0

Not affected by jump

Conclusion:

Absolute world X coordinate.

World Y

Offset:
BASE + 0x9C

Behavior:

Increases when jumping

Returns on landing

Stable while grounded

Conclusion:

Absolute world Y coordinate.

Stage Width

Approximately:

Left wall: -8.5
Right wall: +8.5
Total width ≈ 17 units

State Flags

Offset:
BASE + 0x60
Example: 0x9246BA20

Observed values:

0x0400 → grounded idle
0x0002 → air
0x0401 → moving
0x0421 → moving + opponent contact
0x4401 → right wall
0x8401 → left wall
0x4400 → idle right wall
0x8400 → idle left wall

Bit deductions:

0x0001 → moving
0x0002 → airborne
0x0020 → opponent contact
0x0400 → grounded
0x4000 → right wall
0x8000 → left wall
Additional Horizontal Metric

Offset:
BASE + 0x8C

Behavior:

Increments toward right wall

Decrements when moving away

Conclusion:

Camera-relative or boundary metric.
Not absolute world X.

Velocity

Offset:
BASE + 0x1D0

Behavior:

Increases when moving

Returns to baseline when stopped

Conclusion:

Horizontal velocity component.

Actor Slot Architecture

Static MEM2 slot bases:

Slot 0: 0x9246B9C0
Slot 1: 0x927EB9E0
Slot 2: 0x92B6BA00
Slot 3: 0x92EEBA20

Approximate slot stride:

~ 0x380020

Properties:

Fixed across character swaps

Fixed across stage swaps

Fixed across rematches

Fixed across reloads

Not heap allocated

Not relocated

Conclusion:

Engine-level preallocated actor banks:

ActorSlots[4]

Dynamic fighter detection:

for slot in SLOTS:
    if state & (0x0400 | 0x0002):
        slot is active

No memory scanning required.

Subaction Runtime Block

Region located around:

0x9246BBxx (Slot 0)

This region represents gameplay-layer subaction runtime state.

Not bone data.
Not animation blend data.

Collision State

Address:
0x9246BBE6

Type:
u16 (big endian)

Observed values:

0x0018 → no active hitbox
0x0019 → offensive hitbox active

Behavior:

0x19 persists for entire active duration

Returns to 0x18 immediately when hitbox clears

Deterministic across normals, aerials, specials, supers

Conclusion:

Offensive collision enable state.

Not a pulse.
Not a counter.
Mode flag.

Subaction Frame Counter

Located near collision state.

Behavior:

Increments every frame during startup

Continues incrementing on whiff

Pauses during hitlag

Resumes after hitlag ends

Engine model:

if (!hitlag)
    frame++

Conclusion:

Subaction Frame Index.

Subaction ID

Offset:
CollisionState - 0x24

Behavior:

Changes exactly when animation changes

Deterministic per move

Conclusion:

Subaction / Animation ID field.

Engine Behavioral Model

Per-frame logic inferred:

SubactionFrame++

if SubactionFrame enters active window:
    CollisionState = 0x19
else:
    CollisionState = 0x18

if hit connects:
    Hitlag active
    SubactionFrame paused

Matches all observed runtime behavior.

Subaction Runtime Struct (Partial)

Relative to 0x9246BBE6:

[ -0x24 ]  Subaction ID
[ nearby ] Subaction Frame
[ nearby ] Hitlag Timer
[ +0x00 ]  Collision State (18 / 19)

Precise offsets for frame + hitlag pending full mapping.

Capabilities Enabled by This Mapping

Deterministic memory-side detection of:

Startup frames

Active frames

Recovery window

Hitlag

Whiff vs hit

Move window timing

No need for:

Bone scanning

GX matrix analysis

Breakpoints

PPC tracing
Layer reached

Gameplay runtime execution layer.

This is the correct abstraction tier for frame-data extraction and hitbox timing analysis.

CONFIRMED FIELD MAP (FLAG @ 0x9246BBE6)

FLAG+40 (0x9246BC0E) u16 — BASE_DAMAGE
720=LK, 800=LP, 1360=MK, 1440=MP, 2400=HK, 2480=HP

FLAG+48 (0x9246BC16) u16 — LAST_ACTIVE_FRAME
(12/17/21 for L/M/H)

FLAG+64 (0x9246BC26) u16 — FIRST_ACTIVE_FRAME (STARTUP)
(6/9/12 for L/M/H)

FLAG+52 (0x9246BC1A) u16 — PREV_MOVE_LAST_FRAME
carry-forward (likely cancel logic)

FLAG+60 (0x9246BC22) u16 — PREV_MOVE_FIRST_FRAME
carry-forward

FLAG+6 (0x9246BBEC) f32 — HITBOX_HEIGHT_TYPE
2.0 = mid/high, 3.0 = low

FLAG-56 (0x9246BBAE) u16 — ATTACK_ID
hi = 0x01 when active, lo = sequence index 0–255

HURTBOX / COLLISION CLUSTER (likely world-space, stage-center referenced)

Observed collision block behaves like persistent collision volumes rather than “spawned per-move hitboxes”:

Does not mirror/flip with facing

Values appear measured from stage center / world X, not local/bone space

Updates continuously with animation/pose (attack poses “shift” centers forward without root translation)

Persists across idle / walk / jump, suggesting baseline collision/hurt volumes

Importantly: slots do not clear/revert once written, suggesting a fixed collision-volume array that is selectively overwritten rather than rebuilt per move

Working hypothesis (not proven):
This looks consistent with an always-on set of hurtbox/collision spheres/capsules whose centers follow animation bones, but are stored/accumulated in world coordinates.

Note: not yet proven — needs controlled tests (turnaround, lock-root, bone isolate, freeze anim) to fully confirm coordinate space + attachment.

BONE CLUSTER SUSPECT (possible world-space hitbox positions)

0x9246BA4C (-410) f32 idle≈3.18, active≈3.4–3.8 (12-byte stride)
0x9246BA58 (-398) f32 same
0x9246BA64 (-386) f32 same
0x9246BA70 (-374) f32 same
0x9246BA7C (-362) f32 same
0x9246BAA0 (-326) f32 same

→ Cross-reference with bone labeler: which arm/fist bones live here?

Additional candidate flag

0x9246DA9B
Monitors attacks on ground before opponent is airborne. Resets on new combo. Also appears to reset when a normal can’t be cancelled further (tentative).

CONFIRMED FIELD MAP
FLAG @ 0x9246BBE6
FLAG+40 (0x9246BC0E) — u16 BASE_DAMAGE
720  = LK  
800  = LP  
1360 = MK  
1440 = MP  
2400 = HK  
2480 = HP
FLAG+48 (0x9246BC16) — u16 LAST_ACTIVE_FRAME

Observed:

12 / 17 / 21 (L/M/H normals)

FLAG+64 (0x9246BC26) — u16 FIRST_ACTIVE_FRAME (Startup)

Observed:

6 / 9 / 12 (L/M/H normals)

FLAG+52 (0x9246BC1A) — u16 PREV_MOVE_LAST_FRAME

Likely carry-forward window
Suspected involvement in cancel logic.

FLAG+60 (0x9246BC22) — u16 PREV_MOVE_FIRST_FRAME

Carry-forward (startup reference)
Likely used for chained cancel validation.

FLAG+6 (0x9246BBEC) — f32 HITBOX_HEIGHT_TYPE
2.0 = mid/high  
3.0 = low
FLAG-56 (0x9246BBAE) — u16 ATTACK_ID

High byte = 0x01 when active

Low byte = sequence index (0–255)

Appears to track move slot identity.

COLLISION / HURTBOX CLUSTER (World-Space Suspect)

Observed block behaves like persistent collision volumes rather than dynamically spawned attack-only hitboxes.

Observations

Does not mirror/flip with facing.

Appears measured from stage center / world X, not local bone space.

Updates continuously with animation pose.

Attack poses shift volume centers forward without root translation.

Persists across idle / walk / jump.

Slots do not clear automatically after move end.

Volumes appear selectively overwritten rather than rebuilt per move.

Working Hypothesis (Not Yet Proven)

This structure appears consistent with:

A fixed collision-volume array (likely spheres/capsules)
Whose centers follow animation bones
Stored in world coordinates
Persistently allocated per character

These may represent:

Hurtboxes

Collision volumes

Or a shared slot system for both

Controlled testing still required to confirm:

World vs local transform source

Bone attachment

Overwrite logic

Slot reuse pattern

BONE CLUSTER SUSPECT

Possible world-space volume positions:

0x9246BA4C (-410) f32 idle≈3.18, active≈3.4–3.8 (12-byte stride)
0x9246BA58 (-398)
0x9246BA64 (-386)
0x9246BA70 (-374)
0x9246BA7C (-362)
0x9246BAA0 (-326)

Pattern suggests structured array.

Action item:
Cross-reference with bone labeler to determine which skeletal nodes map to these offsets.

ADDITIONAL FLAG CANDIDATE
0x9246DA9B

Observed behavior:

Monitors grounded attack state before opponent airborne.

Resets on new combo.

Also appears to reset when a normal becomes non-cancellable.

Tentative interpretation:
Possible combo state / ground-confirm tracking / cancel-window validator.

Further testing required.

possible projectile pattern 00 00 00 0C FF FF FF FF ?? ?? ?? ?? ?? ?? ?? ?? 00 00 00 00 ?? ?? ?? ?? ... [hi] [lo] C8 00 00

+0x00   ??? id / flags
+0x??   00 00 00 0C
+0x??   FF FF FF FF
+0x??   speed (41 20 00 00 = 10.0)
+0x??   accel (3F 80 00 00 = 1.0)
+0x??   00 00 00 00
+0x??   cap (41 20 00 00)
+0x50   radius (42 C8 00 00)

0x916BC000 page — Hit Particle FX Data

Page classified as: revert-like (travel + T3==T0) ✓
Contains heap allocator block chain (HEAPHEAD magic)
RGBA color keyframes at 0x916BC1F0+:

FF 7A 00 FF — opaque orange
FF FF 64 FF — yellow
FF 55 55 FF — pinkish-red

Session Notes — TvC Hadouken Position Investigation

Address of interest: 0x91987600

Observed to change when a hadouken is active on screen
Three floats at this address read approximately (4.54, 0.998, -0.005) during an observed throw
The first word (+0x00) appears to be the axis that changes most across a throw — increases by roughly 1.65 per frame
The second word (+0x04) stays near ~1.0 throughout — possibly height
The third word (+0x08) stays near ~0.0 throughout — possibly depth/lateral
max observed per-frame delta: ~1.65 units
observed travel over one throw: ~1.77 units net (short window), pointer slot 0x918A4040 observed holding 0x91987600 consistently across multiple reads

Pointer at 0x918A4040

Consistently read as 0x91987600 across all polling sessions
Did not appear to advance or change — may be a static reference

Spawn reference at 0x918A4004

Read as (0.000, 5.769, 0.998) at one point during the session
Unclear relationship to 0x91987600 — may be a spawn offset or separate struct field

0x918A3FE4

Contains a pointer at +0x060 that pointed to 0x919876C0
0x919876C0 contains a sequence of smoothly incrementing vec3s — observed values like (5.486, 0.869, 0.020) incrementing across rows
Unclear if this is pre-baked or live-written

0x919876C0 block

Observed to contain vec3s with a smoothly changing dominant component across ~0xC0 bytes
Values look trajectory-like but were static across repeated reads in later sessions — may only be valid at spawn time

0x9182B700 region

Contains HEAPHEAD magic
Observed pointer to 0x919874C0 (the spawn scratch arena)
Contains a large linked structure with vtable-like 0x8047Axxx pointers and cross-references into 0x918A3xxx pages

0x919874C0 spawn scratch arena

Contains HEAPHEAD
Observed tiling vec3 pattern (5.014, 1.055, -0.069) repeating across ~0x1B0 bytes — possibly geometry/hitbox verts
Reverts to pre-spawn state after despawn (T3==T0 in MEM2 classifier)

0x916BC000 page

Contains HEAPHEAD + heap block chain
RGBA values observed at 0x916BC1F0: FF7A00FF, FFFF64FF, FF5555FF — orange, yellow, pinkish-red
Pointer at 0x916BC110 to 0x91572460 — unexamined
Tentatively looks like hit particle / visual FX data rather than position

0x90BE4A80 projectile object

High-frequency pointer changes at +0x008/00C/014 — looks like vtable slots swapping on state transitions
Counter at +0x06C read as 0x00000001 — did not increment during observed flight
+0x1E0 (0x90BE4C60) observed flashing during active hadouken — read as code pointer 0x80120B08, not a float
Overall struct appears to be a behavior/logic object rather than a position holder

General observations

The game appears to use a pooled object system — 0x90BE4A80 showed signs of being reused across projectiles
Multiple HEAPHEAD arenas observed linked together across the 0x918xxxxx and 0x919xxxxx ranges
The waypoint buffer hypothesis (pre-baked path, pointer walks through it) was not confirmed — pointer at the cursor slot did not advance during flight
The 0x91987600 region is still the strongest observed candidate for live position data but alignment ambiguity remains — the same three float values appear at +0x00, +0x04, +0x08 in a rotating pattern suggesting the true vec3 base may be one word off

Projectile system notes

Projectile manager head
Address: 9246FD10
Behavior:
When no projectile exists, the pointers at this address reference itself.
When a projectile is active, the pointers change to a different address.
On despawn or hit, it returns to referencing itself.

Conclusion:
This is a circular linked list head for projectiles.

Active projectile node
When L Hado is active, the manager head points to:
91A6CCBC

This address is the active projectile instance node.
When the projectile despawns, it is unlinked and the head resets to itself.

Projectile instance struct
Region begins around:
91A6CCB0

Confirmed inside this struct:

Linked list metadata at the beginning.
Back reference to 9246FD10.
Collision activation timer at 91A6CD24.

Collision timer
Address: 91A6CD24
Default value: BF800000 which is -1.0 as a big endian float.
It decreases over time.

When set to 3F800000 which is 1.0:
Projectile still moves and renders.
Projectile passes through the opponent.
No hit detection occurs.

Conclusion:
This field controls collision activation or hit window state, not lifetime.

Implications about struct layout
This struct contains:
List node pointers
Position vector
Velocity vector
Collision timer
Likely a separate lifetime timer
Likely damage and flags
Likely owner pointer

Architecture summary
9246FD10 is the projectile manager head.
It links to 91A6CCBC when a projectile is active.
That address is the live projectile instance.

This provides deterministic access to projectile objects without scanning.

Open items not yet identified
Exact lifetime timer
Velocity vector offsets
Damage value
Owner pointer offset
Projectile type identifier

Checkpoint
You now have direct access to the live projectile instance via the manager head.
Future work should begin from 9246FD10 and follow the pointer to the active node.

Slot 1 Projectile Manager

Head Address:
0x9246FD10

Behavior:
When empty:
next == 0x9246FD10
prev == 0x9246FD10

When active:
next == projectile_node_address
prev == projectile_node_address

Observed node pool addresses:
0x91A6B778
0x91A6CCBC

These are pooled projectile instance slots.
They are reused between spawns.
They live in the 0x91A6xxxx region.

Confirmed inside node struct:

Linked list metadata at start

Collision activation timer (example previously at 0x91A6CD24)

Position and velocity floats nearby

Node unlinks on despawn

Head resets to self on despawn

Architecture:
Slot 1 head → Slot 1 projectile pool (0x91A6xxxx)

Slot 2 Projectile Manager

Head Address:
0x9246B9C0

Confirmed by:
Node 0x918A15E8 contains:
head: 0x9246B9C0

Observed node pool addresses:
0x918A15E8
Possibly 0x9189ED88 (though that one appears inactive or unrelated)

Slot 2 projectile pool lives in:
0x918Axxxx region

Node layout mirrors Slot 1 structure:

prev pointer

next pointer

head pointer

gameplay fields follow

Architecture:
Slot 2 head → Slot 2 projectile pool (0x918Axxxx)

Structural Conclusions

Each player slot has its own projectile manager head.

Each slot has its own projectile memory pool region.

Pools are separate between Slot 1 and Slot 2.

Nodes are reused, not dynamically allocated per spawn.

Heads are located near actor memory (0x9246xxxx region).

Pools are located in MEM2 (0x91xxxxxx region).

Actor System / Object Architecture (Confirmed Extension)

The projectile system discovered appears to be part of a broader actor-object framework used by the engine.

Evidence supporting this:

Projectile instances exist as nodes in a circular linked list

Nodes are reused from a fixed memory pool

The manager head exists near fighter actor memory

The node pool exists in MEM2 pooled object memory

This is consistent with a typical GameCube-era Actor Manager + Pool Allocator design.

Generalized structure inferred:

ActorManager
{
    ActorListHead
    ActorPool
}

Projectile manager appears to be a specialized actor list rather than a completely separate system.

Projectile Manager Head Behavior (Refined)

Address example:

0x9246FD10

Observed structure:

struct ListHead
{
    Node* next
    Node* prev
}

Behavior:

Idle state:

next = head
prev = head

Active state:

next = node
prev = node

This confirms a circular doubly-linked list sentinel node.

Properties:

Head never moves

Nodes are inserted and removed

Empty list always points to itself

This design allows constant-time insert/remove operations.

Projectile Node Structure (Expanded)

Observed node example:

0x91A6CCBC

Memory preceding this indicates the node likely begins at:

0x91A6CCB0

Initial layout likely resembles:

struct ProjectileNode
{
    Node* prev        // linked list
    Node* next
    Node* head        // back-reference to manager head

    ActorVTable* vtbl
    Flags / state
    Owner pointer

    vec3 position
    vec3 velocity

    float collision_timer
    float lifetime_timer

    damage
    flags
}

Evidence supporting this:

Node contains back-reference to manager head

Multiple float fields update during flight

Collision timer field behaves independently from lifetime

Node unlinks from list when projectile despawns

Object Pool Behavior

Projectile instances appear to be drawn from a fixed memory pool.

Example pool region:

0x91A6xxxx

Observed pool nodes:

0x91A6B778
0x91A6CCBC

Properties:

Node addresses are reused between spawns

Nodes remain allocated even when inactive

Only linked-list membership changes

This indicates a preallocated object pool, not dynamic heap allocation per projectile.

Typical implementation pattern:

ProjectilePool[N]

for node in pool:
    if node.active == false:
        reuse node

Advantages of this design:

No runtime allocation cost

Deterministic memory layout

Faster insertion/removal

No fragmentation

Per-Slot Projectile Managers

Evidence suggests each fighter slot maintains its own projectile manager.

Observed heads:

Slot 1 head → 0x9246FD10
Slot 2 head → 0x9246B9C0

Observed pools:

Slot 1 pool → 0x91A6xxxx
Slot 2 pool → 0x918Axxxx

This suggests the architecture:

FighterActor
{
    ProjectileManager
}

Meaning projectile ownership is likely tied to the fighter actor instance.

Benefits of this architecture:

Projectiles are automatically associated with their owner

Cleanup on character death / KO becomes trivial

Multi-character fights remain deterministic

Collision Timer Interpretation

Address example:

0x91A6CD24

Default:

BF800000  →  -1.0

Observed behavior:

Counts downward or changes during active frames

When forced to 1.0, projectile becomes non-hitting but still moves

Conclusion:

This field likely represents collision enable state rather than lifetime.

Possible logic model:

if collision_timer >= 0:
    collision_enabled
else:
    collision_disabled

This may control:

first-hit activation

invulnerability windows

multi-hit cooldown

Relationship to Fighter Actor Struct

Projectile manager heads appear located near fighter actor memory.

Example:

FighterActorBase
    + offsets
        ProjectileManagerHead

This suggests projectile managers may be embedded inside the fighter actor struct rather than global.

Possible pattern:

struct FighterActor
{
    ...
    ProjectileManager manager
}

This would naturally partition projectile ownership by slot.

Actor Pool Memory Regions

Two primary MEM2 object regions have been observed:

0x91A6xxxx
0x918Axxxx

Both contain:

object-like structs

linked list nodes

float motion data

pointer references into actor code

This indicates MEM2 likely hosts multiple actor pools used for:

projectiles

assists

temporary entities

effects

Relationship to Spawn System

Earlier observations of spawn command buffers and heap activity may correspond to a higher-level spawn system that activates nodes in these pools.

Possible flow:

Move script triggers spawn
      ↓
Spawn command written
      ↓
Actor manager activates node from pool
      ↓
Node linked into projectile list

Once active, the projectile node is updated each frame by the actor system.

Overlay / Tooling Implications

Because the projectile manager provides deterministic access to live nodes:

head -> next -> next -> ...

You can enumerate projectiles reliably without memory scanning.

Typical traversal:

node = head.next
while node != head:
    process(node)
    node = node.next

This allows:

live projectile tracking

hitbox overlay

velocity extraction

owner identification

Open Mapping Targets

Fields still unidentified inside projectile node:

lifetime timer

owner pointer

damage value

projectile type identifier

hitbox radius

hitlag parameters

cancel / interaction flags

These likely exist within the node following the position/velocity fields.

Summary of Confirmed Architecture

FighterActor
    ProjectileManagerHead
        ↓
    Circular linked list

ProjectilePool (MEM2)
    Node structs reused

ProjectileNode
    prev
    next
    head
    position
    velocity
    collision timer
    other gameplay fields

Projectiles are therefore actor-style objects managed through per-slot linked lists backed by fixed pools.

Projectile Node Pool

Base address:

0x919874C0

Stride:

0x30

Observed active nodes during hadouken:

0x919874F0
0x91987520
0x91987550
0x91987580
0x91987610
0x91987640
0x91987670
0x919876A0

These repeat between projectiles, indicating a fixed object pool.

Estimated pool size:

~64 entries
Projectile Node Layout (Partial)

Confirmed transform offsets:

+0x10  float pos_x
+0x14  float pos_y
+0x18  float pos_z

Example motion data:

x ≈ -0.15 → 0.31
y ≈ -3 → -5
z ≈ 0.6 → 1.0

These match TvC world-space coordinates.

Node Header

Nodes appear to begin with a vtable pointer:

+0x00  0x9182B700

Likely structure start:

struct ProjectileNode
{
    void* vtable
    ProjectileNode* next
    ProjectileNode* prev
    ...
    vec3 position
}

Observed internal linked-list pointers:

node+0x04
node+0x08
Spawn Routing System

Runtime route pointer observed at:

0x918A3EC0

Values during hadouken:

0x918A417C
0x918A4260
0x918A4098

Interpretation:

route pointer → move/actor script entry

Example mapping:

Ryu hadouken route → 0x918A417C
Ken hadouken route → 0x918A4260

These routes change before projectile nodes activate.

Pipeline confirmed:

fighter move
    ↓
route pointer (0x918Axxxx)
    ↓
spawn actor
    ↓
projectile node pool allocation
Heap Allocator Regions

Two heap headers identified:

0x91987920
0x918A3EA0

Signature:

"HEAPHEAD"

Pointers from these regions into the projectile pool are allocator metadata, not gameplay logic.

Memory Reference Scan Results

References to projectile pool found at:

0x9182B6FC -> 0x919874C0
0x9182B70C -> 0x919874C0

Likely associated with the projectile class or manager vtable.

Other references near:

0x918A3F5C – 0x918A42F0

These are allocator bookkeeping entries.

Current Overlay Strategy

Reliable approach:

scan projectile pool
read transform
draw hitbox

Example logic:

node = 0x919874C0 + i*0x30

if rd32(node) == 0x9182B700:
    x = rf(node+0x10)
    y = rf(node+0x14)
    z = rf(node+0x18)

This avoids relying on the route system.

Subactor / Projectile System Notes
Node Transform Offsets

Projectile node structure exposes transform data at:

node + 0x04
node + 0x08

(Additional transform offsets also observed later in the struct.)

Typical usage during overlay:

x = rf(node + 0x10)
y = rf(node + 0x14)
z = rf(node + 0x18)
Spawn Routing System

Runtime route pointer observed at:

0x918A3EC0

Observed values during Hadouken execution:

0x918A417C
0x918A4260
0x918A4098
Interpretation
route pointer → move / actor script entry

Example mapping:

Ryu Hadouken route → 0x918A417C
Ken Hadouken route → 0x918A4260

These route pointers change immediately before projectile nodes activate.

Confirmed Spawn Pipeline
fighter move
     ↓
route pointer (0x918Axxxx)
     ↓
spawn actor
     ↓
projectile node pool allocation

This confirms the route system is responsible for dispatching the actor creation event.

Heap Allocator Regions

Two heap headers identified:

0x91987920
0x918A3EA0

Signature:

"HEAPHEAD"

Pointers from these regions into the projectile pool are allocator metadata, not gameplay logic.

These should be ignored when tracing gameplay ownership.

Memory Reference Scan Results

References into the projectile pool located at:

0x9182B6FC → 0x919874C0
0x9182B70C → 0x919874C0

These likely belong to:

projectile class
projectile manager
vtable reference

Additional allocator references found around:

0x918A3F5C – 0x918A42F0

These appear to be heap bookkeeping structures, not actor logic.

Projectile Node Pool

Observed pool base:

0x919874C0

Observed stride:

0x30 bytes per node

Example access:

node = 0x919874C0 + i * 0x30
Current Reliable Overlay Strategy

The most stable approach is to scan the node pool directly.

Example logic:

node = 0x919874C0 + i * 0x30

if rd32(node) == 0x9182B700:

    x = rf(node + 0x10)
    y = rf(node + 0x14)
    z = rf(node + 0x18)

    draw_hitbox(x, y, z)

Advantages:

✔ does not depend on spawn route system
✔ works even if routing changes
✔ stable across move scripts
Key Takeaway

The route system only triggers actor creation.

Once the actor exists:

projectile node pool → authoritative position source

Therefore the overlay should read node transforms directly, not attempt to follow routing logic.

Projectile Node Pools

Initial projectile node pool observed at:

0x91B15A10

Node stride:

0x30 bytes

Observed nodes:

0x91B15A10
0x91B15A40
0x91B15A70

Structure pattern strongly suggests:

node = base + index * 0x30

Likely fields:

node+0x10  → X position
node+0x14  → Y position
node+0x18  → Z position

These match expected float ranges for world coordinates.

Secondary Node Pool

A second table was discovered further down in memory:

0x91B15B50

This table appears when:

multiple projectiles exist

multiple characters spawn actors simultaneously

primary table becomes inactive

Example case:

Ryu Hadouken
+
Morrigan Soul Fist

Result:

0x91B15A10 table stops updating
0x91B15B50 becomes active

This indicates multiple actor pools or a double-buffered system.

Pool Layout Observation

Memory layout suggests contiguous pool blocks:

0x91B15980  pool header
0x91B15A10  pool A
0x91B15B50  pool B

Pointer relationships observed:

91B15B50 → references addresses near 91B159B0

Which suggests:

allocator metadata
linked pool blocks
Actor Node Structure Indicators

Several values inside the nodes resemble transform components.

Examples:

3F800000 ≈ 1.0
3F7FFFFE ≈ ~0.9999

These are typical for:

scale
rotation matrix values
transform normalization

This strongly suggests nodes contain full transform data.

Likely structure (partial):

+00  actor/vtable pointer
+04  flags/state
+08  pointer/reference
+10  pos_x
+14  pos_y
+18  pos_z
+1C  rotation / velocity / scale

Further confirmation required.

Actor Spawn Pipeline (Confirmed)

Move execution triggers actor spawn via route script.

Pipeline:

fighter move
   ↓
route pointer (0x918Axxxx)
   ↓
spawn actor
   ↓
actor allocated in projectile node pool

Example route pointers observed:

0x918A417C   Ryu Hadouken
0x918A4260   Ken Hadouken
0x918A4098   alternate route

Runtime route pointer location:

0x918A3EC0
Heap Allocator

Heap headers discovered:

0x91987920
0x918A3EA0

Signature:

"HEAPHEAD"

These manage memory for projectile pools but are not gameplay structures.

Reliable Overlay Strategy

Best approach for rendering projectile hitboxes:

scan projectile pool nodes
detect active nodes
read transform
draw hitbox

Example logic:

node = pool_base + i * 0x30

if rd32(node) == actor_class_ptr:
    x = rf(node + 0x10)
    y = rf(node + 0x14)
    z = rf(node + 0x18)

This avoids dependency on route scripts.

Important Finding

Projectile pool location is not static.

Observed behavior:

pool A active
→ multiple actors spawn
→ pool B becomes active
→ pool A stops updating

Meaning the engine likely uses:

multiple pools
or
double-buffered actor tables

Overlay should therefore monitor multiple pool bases.

Candidate Pool Bases

Current confirmed ranges:

0x91B15A10
0x91B15B50

Both should be scanned.